
public class Helpers {

        public static string getExecutableDirectory() {
           	string Path;
           	//TODO call winapi functions (create vapi for winapi !? )
        	//Path = Path.slice(0,Path.last_index_of_char('/'));
        	return Path;
        }
        
}

